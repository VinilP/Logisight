pandas>=1.5.0
numpy>=1.21.0
python-dotenv>=0.19.0

# Optional LLM Integration Dependencies
# Install based on your preferred approach:

# OPTION A: OpenAI API Integration (requires API key)
# openai>=1.0.0

# OPTION B: Local LLM with Ollama (no API key needed)
# ollama>=0.1.0

# Both options are optional - system works with rule-based approach as fallback